# eit-db 版本管理策略

## 📋 分支策略

采用 **Release Branch Model** 进行版本管理，结合 git tags 确保版本可追溯。

### 分支结构

```
main (生产分支)
  ├── 始终保持最新稳定版本
  ├── 从发布分支合并
  └── 对应版本标签（v0.1.0, v0.1.4, v0.2.0 等）

release/vX.Y.Z (发布分支)
  ├── 为每个发布版本创建
  ├── 从 main 分支（当时的 HEAD）创建
  ├── 用于紧急修复和版本维护
  └── 永久保留，不删除

develop/vX.Y.Z (开发分支)
  ├── 新功能开发分支
  ├── 完成后合并到 main
  └── 可删除（仅保留历史记录）
```

## 🔄 工作流

### 新版本开发

```
1. 创建开发分支
   git checkout -b develop/v0.3.0 main

2. 进行功能开发和测试
   git commit -m "feature: ..."
   git push origin develop/v0.3.0

3. 版本就绪时，合并到 main
   git checkout main
   git merge --no-ff develop/v0.3.0
   git tag v0.3.0

4. 从 main 创建发布分支
   git checkout -b release/v0.3.0 main
   git push origin release/v0.3.0

5. 推送所有分支和标签
   git push origin main
   git push origin release/v0.3.0
   git push origin --tags
```

### 紧急修复（Hotfix）

如果需要在已发布版本上修复 bug：

```
1. 从发布分支创建修复分支
   git checkout release/v0.2.0
   git checkout -b hotfix/v0.2.1

2. 修复 bug 并提交
   git commit -m "fix: ..."

3. 发布新的补丁版本
   git tag v0.2.1

4. 推送修复分支和标签
   git push origin hotfix/v0.2.1
   git push origin --tags

5. （可选）将修复合并回 main
   git checkout main
   git merge --no-ff hotfix/v0.2.1
```

## 📌 版本标记

### 标签命名规则

- **正式版本**: `vX.Y.Z` (如 v0.2.0)
- **预发布版本**: `vX.Y.Z-alpha`, `vX.Y.Z-beta` (如 v0.3.0-beta)
- **修订版本**: `vX.Y.Z` (如 v0.2.1 - 补丁修复)

### 标签创建

```bash
# 正式版本标签
git tag -a v0.2.0 -m "v0.2.0 - 定时任务抽象层

核心特性:
✓ ScheduledTask 抽象
✓ Adapter 接口扩展
..."

# 推送标签
git push origin v0.2.0
git push origin --tags  # 推送所有标签
```

## 🎯 当前分支结构（2026-02-03）

```
主线 (main)
  └─ 89c7488 Merge branch 'develop/v0.2.0' into main
      ├─ f324421 docs: 添加 v0.2.0 发布说明
      └─ 18002e6 (tag: v0.2.0) feat: v0.2.0 - 定时任务抽象层
         └─ 0815cf3 fix: 更新 module 名字
            └─ 504ac22 docs: 添加 v0.1.4 发布说明
               └─ 574e4d4 (tag: v0.1.4) fix: v0.1.4 核心修复

发布分支
  └─ release/v0.1.4 (0815cf3)  # 保留 v0.1.4 的快照

开发分支
  └─ develop/v0.2.0 (f324421)  # v0.2.0 开发分支
```

## 📊 分支映射表

| 分支 | HEAD 提交 | 对应版本 | 用途 |
|------|---------|---------|------|
| main | 89c7488 | v0.2.0 | 生产分支，最新稳定版 |
| release/v0.1.4 | 0815cf3 | v0.1.4 | v0.1.4 版本快照 |
| develop/v0.2.0 | f324421 | v0.2.0 | v0.2.0 开发记录 |

## 🔍 查询版本信息

### 查看所有版本

```bash
git tag -l              # 列出所有标签
git tag -l -n 5         # 列出标签和注解信息
```

### 查看特定版本的提交

```bash
git log v0.2.0 --oneline -5      # 查看 v0.2.0 的提交
git log v0.1.4..v0.2.0 --oneline # 查看 v0.1.4 到 v0.2.0 的变更
```

### 检出特定版本

```bash
git checkout v0.2.0                # 基于标签检出代码
git checkout release/v0.1.4        # 基于分支检出代码
```

## 🎓 后续版本管理

### 下一个版本（v0.3.0）示例

```bash
# 1. 创建开发分支
git checkout -b develop/v0.3.0 main
git push -u origin develop/v0.3.0

# 2. 开发完成后，合并回 main
git checkout main
git merge --no-ff develop/v0.3.0
git tag -a v0.3.0 -m "v0.3.0 - 新功能说明..."

# 3. 创建发布分支
git checkout -b release/v0.3.0 main
git push origin release/v0.3.0

# 4. 推送所有分支
git push origin main
git push origin --tags
```

## 📈 版本历史

| 版本 | 发布日期 | 分支 | 主要变更 |
|------|---------|------|---------|
| v0.2.0 | 2026-02-03 | release/v0.2.0 | 定时任务抽象层 |
| v0.1.4 | 2026-02-02 | release/v0.1.4 | GetGormDB() 修复，PostgreSQL 认证 |
| v0.1.3 | 之前 | - | 早期版本 |

## ✅ 版本管理最佳实践

1. **main 分支始终可部署** - 仅包含通过测试的代码
2. **每个发布版本创建 release 分支** - 便于后续维护
3. **使用 annotated tags** - 记录版本信息和发布日期
4. **清晰的提交信息** - 便于追溯变更历史
5. **开发分支独立开发** - 不影响主线稳定性

## 🔗 相关文档

- **v0.2.0 发布说明**: `.dev-docs/v0.2.0_RELEASE_NOTES.md`
- **v0.2.0 设计文档**: `.dev-docs/v0.2.0_DESIGN.md`
- **v0.2.0 完成报告**: `.dev-docs/v0.2.0_COMPLETION.md`

---

**文档版本**: 1.0  
**最后更新**: 2026-02-03  
**维护者**: eit-db Agent
